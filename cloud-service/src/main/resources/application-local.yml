# 本地开发配置
# 启动方式: --spring.profiles.active=local
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_food?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: admin123
  redis:
    host: localhost
    port: 6379
    password:
    ssl: false

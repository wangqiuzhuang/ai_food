<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.SwUserMapper">

    <select id="findByUserName" resultType="com.example.entity.SwUser">
        SELECT * FROM sw_user WHERE user_name = #{userName}
    </select>
</mapper>
